{% extends 'base_back.html.twig' %}

{% block content %}
    <h1>{{ article.title }}</h1>
    <p>
        {{ article.content }}
    </p>
    <p>
        {{ article.createdAt|date('Y-m-d') }} à {{ article.createdAt|date('H:m:i') }}
    </p>

{% endblock %}