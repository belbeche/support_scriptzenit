{% extends 'base_back.html.twig' %}

{% block content %}
    <div class="container">

        <div class="card">
            {% for image in article.images %}
                <img src="{{ asset('uploads/' ~ image.name) }}" alt="{{ article.title }}" width="150">
            {% endfor %}
        </div>

        <h1>{{ article.slug }}</h1>
        <p>
            {{ article.content|raw }}
        </p>
        <p>
            Crée le : {{ article.createdAt|format_datetime('full', 'short', locale='fr') }}
        </p>
        <p>
            Modifier le :  {{ article.updatedAt|format_datetime('full', 'short', locale='fr') }}
        </p>

        <a href="{{ url('front_articles_show', {slug: article.slug}) }}">Voir l'article</a>
    </div>

{% endblock %}